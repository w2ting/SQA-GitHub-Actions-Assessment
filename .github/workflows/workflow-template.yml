name: Flask CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      # TODO: Add your workflow steps here
      # Hint: You'll need to:
      # 1. Checkout code
      # 2. Setup Python
      # 3. Install dependencies
      # 4. Run pytest
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # Step 4: Run the tests
      - name: Run pytest
        run: pytest -v
      
  notify:
    runs-on: ubuntu-latest
    if: always()
    needs: [test]
    steps:
      # TODO: Add Discord notification steps here
      # Hint: Use a Discord webhook action or curl
      - name: Send Discord notification
        uses: tsickert/discord-webhook@v5.3.0
        with:
          webhook-url: ${{ secrets.DISCORD_WEBHOOK_URL }}
          content: "Deployment to main branch successful! 🎉 test by weiting"
          username: "GitHub Actions"
          avatar-url: "https://discord.com/api/webhooks/1441330277279469578/DsysO-Ce_53G5q8Nw8-PBZi9M5P4Hd9SHdcfDa8qEKrGCmqwVZQDPN8j8y8V3WYfaQl0"
